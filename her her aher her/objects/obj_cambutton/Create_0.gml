function draw_camsprite() {
	
	var centerx = (room_width / 2);
	var centery = (room_height / 2);
	
	  draw_sprite_stretched(Sprite2, 0, centerx, centery, room_width, room_height );
	 
	 //if spr.image_number >= 11 { draw
	 
};